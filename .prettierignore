.next
node_modules
coverage

# Machine-local editor state. Ignored by a global gitignore rather than this
# repo's, so it never reaches CI, but it still fails `format:check` locally.
.claude/settings.local.json
playwright-report
test-results
public/r
yarn.lock
.yarn
*.png
*.ico

# standalone packages build their own output
packages/*/dist
packages/*/node_modules
